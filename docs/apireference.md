---
id: apireference 
title: APIReference
---

### Setters

#### Monochrome-colors

|  Setter  | Description |
| :-----   | :-----      |
| `x-black` & `y-black` & `z-black` | setter for color black |
| `xh-black` & `yh-black` & ` zh-black` | setter for `:focus` & `:hover` on black color |
| `x-white` & `y-white` & `z-white` | setter for color white |
| `xh-white` & `yh-white` & `zh-white` | setter for `:focus` & `:hover` on white color |
| `x-gray` & `y-gray` & `z-gray` | setter for color gray |
| `xh-gray` & `yh-gray` & `zh-gray` | setter for `:focus` & `:hover` on gray color |

#### Modals-colors

| Setter | Description |
| :----- | :------     |
| `x-error` & `y-error` & `z-error` | setter for error massages |
| `xh-error` & `yh-error` & `zh-error` | setter for `:focus` & `:hover` on error class |
| `x-warning` & `y-warning` & `z-warning` | setter for warning massages or warning inputs | 
| `xh-warning` & `yh-warning` & `zh-warning` | setter for `:focus` & `:hover` on warning class |
| `x-success` & `y-success` & `z-success` | setter for successful inputs |
| `xh-success` & `yh-success` & `zh-success` | setter for `:focus` & `:hover` on success class |

#### Wheel-colors

| Setter | Description |
| :----- | :------     |
| `x-red` & `y-red` & `z-red` | setter for color red |
| `xh-red` & `yh-red` & `zh-red` | setter for `:focus` & `:hover` on red color |
| `x-orange` & `y-orange` & `z-orange` | setter for color orange |
| `xh-orange` & `yh-orange` & `zh-orange` | setter for `:focus` & `:hover` on orange color |
| `x-yellow` & `y-yellow` & `z-yellow` | setter for color yellow |
| `xh-yellow` & `yh-yellow` & `zh-yellow` | setter for `:focus` & `:hover` on yellow color |
| `x-olive` & `y-olive` & `z-olive` | setter for color olive |
| `xh-olive` & `yh-olive` & `zh-olive` | setter for `:focus` & `:hover` on olive color |
| `x-green` & `y-green` & `z-green` | setter for color green | 
| `xh-green` & `yh-green` & `zh-green` | setter for `:focus` & `:hover` on green color | 
| `x-indigo` & `y-indigo` & `z-indigo` | setter for indigo color |
| `xh-indigo` & `yh-indigo` & `zh-indigo` | setter for `:focus` & `:hover` on indigo color |
| `x-azure` & `y-azure` & `z-azure` | setter for azure color |
| `xh-azure` & `yh-azure` & `zh-azure` | setter for `:focus` & `:hover` on azure color |
| `x-blue` & `y-blue` & `z-blue` | setter for color blue |
| `xh-blue` & `yh-blue` & `zh-blue` | setter for `:focus` & `:hover` on blue color |
| `x-violet` & `y-violet` & `z-violet` | setter for color violet |
| `xh-violet` & `yh-violet` & `zh-violet` | setter for `:focus` & `:hover` on violet color |
| `x-pink` & `y-pink` & `z-pink` | setter for color pink |
| `xh-pink` & `yh-pink` & `zh-pink` | setter for `:focus` & `:hover` on pink color |
| `x-transparent` & `y-transparent` & `z-transparent` | setter for the transparent( color of background ) |
| `xh-transparent` & `yh-transparent` & `zh-transparent` | setter for `:focus` & `:hover` on the transparent color |

#### Theme-colors

| Setter | Description |
| :----  | :-------    |
| `x-primary` & `y-primary` & `z-primary` | setter for the primary color of the theme |
| `xh-primary` & `yh-primary` & `zh-primary` | setter for `:focus` & `:hover` on the primary color |
| `x-secondary` & `y-secondary` & `z-secondary` | setter for the secondary color of the theme |
| `xh-secondary` & `yh-secondary` & `zh-secondary` | setter  for `:focus` & `:hover` on the secondary color |
| `x-accent1` & `y-accent1` & `z-accent1` | setter for the accent-1 color ( the color to emphasising, contrasting a theme ) |
| `xh-accent1` & `yh-accent1` & `zh-accent1` | setter for `:focus` & `:hover` on the accent-1 color |
| `x-accent2` & `y-accent2` & `z-accent2` | setter for the accent-2 color ( the second color to use for emphasising, contrasting a theme ) |
| `xh-accent2` & `yh-accent2` & `zh-accent2` | setter for `:focus` & `:hover` on the accent-2 color |
| `x-accent3` & `y-accent3` & `z-accent3` | setter for the accent-3 color ( the third color to use for emphasising, contrasting a theme ) |
| `xh-accent3` & `yh-accent3` & `zh-accent3` | setter for `:focus` & `:hover` on the accent-3 color |

_Note: accent colors can be used all together or in pair or even none at all depending your design needs._

#### SM-colors

SM-colors are designed to use for the social network logos.

| Setter | Description |
| :---   | :-------    |
| `x-twitter` & `y-twitter` & `z-twitter` | setter for twitter social network logo |
| `xh-twitter` & `yh-twitter` & `zh-twitter` | setter for `:focus` & `:hover` on the twitter social network logo |
| `x-facebook` & `y-facebook` & `z-facebook` | setter for facebook social network logo |
| `xh-facebook` & `yh-facebook` & `zh-facebook` | setter for `:focus` & `:hover` on the facebook social network logo |
| `x-instagram` & `y-instagram` & `z-instagram` | setter for instagram social network logo |
| `xh-instagram` & `yh-instagram` & `zh-instagram` | setter for `:focus` & `:hover` on the instagram social network logo |
| `x-google` & `y-google` & `z-google` | setter for google logo |
| `xh-google` & `yh-google` & `zh-google` | setter for `:focus` & `:hover` on the google logo |

#### UI-colors

| Setter | Description |
| :----  | :------     |
| `x-background` & `y-background` & `z-background` | setter for the background of the UI design |
| `xh-background` & `yh-background` & `zh-background` | setter for `:focus` & `:hover` on the background |
| `x-paragraph` & `y-paragraph` & `z-paragraph` | setter for the paragraphs of the UI design |
| `xh-paragraph` & `yh-paragraph` & `zh-paragraph` | setter for `:focus` & `:hover` on the paragraph |
| `x-navlink` & `y-navlink` & `z-navlink` | setter for the navigation links of the UI design |
| `xh-navlink` & `yh-navlink` & `zh-navlink` | setter for `:focus` & `:hover` on the navigation links |
| `x-link` & `y-link` & `z-link` | setter for the links of the UI design |
| `xh-link` & `yh-link` & `zh-link` | setter for `:focus` & `:hover` on the links |
| `x-heading` & `y-heading` & `z-heading` | setter for the headings of the UI design |
| `xh-heading` & `yh-heading` & `zh-heading` | setter for `:focus` & `:hover` on the headings |
| `x-subtitle` & `y-subtitle` & `z-subtitle` | setter for subtitles on the UI design |
| `xh-subtitle` & `yh-subtitle` & `zh-subtitle` | setter for `:focus` & `:hover` on the subtitles |
| `x-metadata` & `y-metadata` & `z-metadata` | setter for the metadatas |
| `xh-metadata` & `y-metadata` & `zh-metadata` | setter for `:focus` & `:hover` on the metadata |

#### Theme changer

| Setter | Description |
| :----  | :-------    |
| `ui-dark` | Setter for the parent element (i.e body) |
| `ui-iceberg` | setter for the parent element (i.e body) |

Note: This setter is designed to be used on the parent element to change the color scheme of the document.

### Getters 

| Getter | Decsription | 
| :----  | :-------    |
| `b-x` & `b-y` & `b-z` | defines a background color of an element |
| `c-x` & `c-y` & `c-z` | defines a forground color of an element | 
| `d-x` & `d-y` & `d-z` | defines a border color of an element |
| `e-x` & `e-y` & `e-z` | fills inside of an element |
| `b-light-x` & `b-light-y` & `b-light-z` | defines the light shade of the  background color |
| `c-light-x` & `c-light-y` & `c-light-z` | defines the light shade of the forground color |
| `e-light-x` & `e-light-y` & `e-light-z` | fills the element with the light shade of the color |
| `b-dark-x` & `b-dark-y` & `b-dark-z` | deifnes the dark shade of the background color |
| `c-dark-x` & `c-dark-y` & `c-dark-z` | defines the dark shade of the forground color |
| `e-dark-x` & `e-dark-y` & `e-dark-z` | fills the element with the dark shade of th the color |
| `b-on-x` & `b-on-y` & `b-on-z` | defines the background color of the child element that has the color of the same setter |
| `c-on-x` & `c-on-y` & `c-on-z` | defines the forground color for it's background color |
| `e-on-x` & `e-on-y` & `e-on-z` | fills the child element of the parent element that has the color of the same setter |

### Mixins of each part of the library

If you rather to input one part of the Swatch to your own library.

Note: It's recommended to use `@use` rule instead of `@import` rule for it's multiple advantages.

| irective      |     Description      |
| :------------ |     :----------      |
| `@include mixins.define-variables(vars.monochrome-colors);` | Adds the monochrome colors to the library |
| `@include mixins.define-variables(vars.modals-colors);` | Adds the modals colors to the library |
| `@include mixins.define-variables(vars.wheel-colors);` | Adds the wheel colors to the library |
| `@include mixins.define-variables(vars.theme.colors);` | Adds the Theme colors to the library |
| `@include mixins.define-variables(vars.sm-colors);` | Adds the SM colors to the library |
| `@include mixins.define-variables(vars.ui-colors);` | Adds the UI colors to the library |
